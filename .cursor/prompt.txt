SVG diagram:
Draw an animated SVG network topology diagram for a typical web application stack. Include: Internet (cloud), Firewall (UTM/IDS), Load Balancer, two Web Servers (10.0.1.10 and 10.0.1.11), a PostgreSQL Database, an IoT Gateway (MQTT Bridge), and an Admin Laptop (marked unauthorized). Use isometric 3D tile nodes in navy blue (#1E3A5F palette), gold arrows for primary data flow, blue dashed arrows for load-balanced traffic, red dotted arrows for the alert/blocked admin connection, and purple dashed for IoT. Animate signal pulses along every connection using SVG animateMotion. Add LED status indicators on each node, a ring ripple on the Internet node, glow pulses on the Firewall and Database, zone backgrounds for Cloud and DMZ areas, and a legend. Layer order: connections first, then animated signals, then nodes on top.
